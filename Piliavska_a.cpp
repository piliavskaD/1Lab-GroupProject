#include "shared_types.h"

Result processStudentA(const InputData& data) {
    std::cout << "[Student A] Processing data...\n";
    Result res;
    res.calculatedValue = 0.0;
    res.isSuccess = true;
    return res;
}